server:
  port: 9500
spring:
  application:
    name: monitoring-server
  boot:
    admin:
      discovery:
        ignored-services:
        - monitoring-server
  cloud:
    config:
      uri:
      - http://localhost:8888
---
spring:
  profiles:
  - docker
  cloud:
    config:
      uri:
      - http://config-server:8888
  