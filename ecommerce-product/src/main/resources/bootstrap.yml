spring:
  application:
    name: ecommerce-products
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: ecommerce-products
            client-secret: 3da07f5e-15ff-489f-aeae-fe66fea24262
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/auth/realms/ecommerce-realm
management:
  endpoints:
    web:
      exposure:
        include:
        - health
        - info
        - metrics
 
